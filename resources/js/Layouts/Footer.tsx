import Box from "@mui/material/Box";
import {blue} from "@mui/material/colors";

export default function Footer() {
    return (
        <Box sx={{background: blue[800], textAlign: "center", color: 'white', padding: 2}}>
            CAME Connect
        </Box>
    );

}
